spring:
  mongodb.embedded.version: "3.6.0"
  data.mongodb.database: library
  main.allow-circular-references: true

mongock:
  runner-type: "InitializingBean"
  mongo-db:
    write-concern:
      journal: false
  change-logs-scan-package:
    - ru.otus.homework08.mongock.changelog.test
  trackIgnored: true